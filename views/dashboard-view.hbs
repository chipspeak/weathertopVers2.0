{{> menu active="dashboard"}}

<div class="content box">
  {{> list-stations}}
    <div id="map">
  </div>
    <script>
      var map = L.map('map').setView([53.3498, -6.2603], 6);
    </script>
  {{#each stations}}
    <script>
  L.tileLayer('https://tile.openstreetmap.org/{z}/{x}/{y}.png', {
    maxZoom: 19,
    attribution: '© OpenStreetMap'
}).addTo(map);
      var marker = L.marker([{{longitude}}, {{latitude}}]).addTo(map);
      var marker = L.marker([52.25833, -7.11194]).addTo(map);
  </script>
{{/each}}
  <button class="button is-size-5 is-light my-5">Add New Station<i class="fa-solid fa-satellite-dish pl-2"></i></button>
  {{> add-station}}